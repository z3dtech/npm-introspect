const requestData = require('../server/requestData');
const assert = require('assert');
const chai = require('chai');


const pkgMisspelled = ['react', 'redux', 'redu']
describe('requestData', function(){
  it('should return an error because name is spelled wrong', function(){



    assert.equal()
  })
})



requestData.parse()

//additional packages
//test what happens when I misspell the packages
//test when we hit 400 error


//look at npm-link
requestData.parse(pkgs)
.then(function (data)

describe('requestData.parse()', function(){
  it('parses data with a misspelled name', function(){
    return
  })
})


  describe('#save()', function() {
    it('should save without error', function(done) {
      user.save(function(err) {
        if (err) done(err);
        else done();
      });
    });
  });
